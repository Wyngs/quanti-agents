<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Top row: title + status on the left, Show QR button on the right -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/text_event_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Event title"
                    android:textStyle="bold"
                    android:textSize="16sp" />

                <TextView
                    android:id="@+id/text_event_status"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="Status"
                    android:textSize="12sp" />
            </LinearLayout>

            <!-- Show QR button styled like button_toggle_qr -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/button_show_qr"
                style="@style/Widget.QuantiAgents.Button.Tonal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:text="@string/manage_events_show_qr"
                app:icon="@drawable/ic_qr_code"
                app:iconPadding="8dp"
                app:iconGravity="textStart" />
        </LinearLayout>

        <!-- Info lines under the header row -->
        <TextView
            android:id="@+id/text_waiting_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Waiting list: 0 entrants"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/text_capacity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Capacity: 0 participants"
            android:textSize="14sp" />

        <!-- Existing Manage Event Info button -->
        <Button
            android:id="@+id/button_manage_event_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/manage_events_manage_info" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
